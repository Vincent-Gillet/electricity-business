spring.application.name=api

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

#jwt.secret-key-access-token=XUqxkJb4qOq+lOKhO3rdM5CkwD+WkCvQ2HYOcsF6bLQ=
jwt.secret-key-access-token=${ACCESS_KEY}
#jwt.secret-key-refresh-token=NjczNjMzMzEzMDM3MjY0MTM3NTI0MDUzMzM3MDIyNTc=
jwt.secret-key-refresh-token=${REFRESH_KEY}

# Paramètres Local

## Deploiement local avec Docker Compose
#spring.datasource.url=jdbc:mysql://localhost:3306/db-electricity-business?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.username=${DATABASE_USERNAME}
#spring.datasource.password=${DATABASE_PASSWORD}

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

## Utilisation clé SSL pour HTTPS
#server.ssl.key-store=classpath:keystore.p12
#server.ssl.key-store-password=azerty
#server.ssl.key-store-type=PKCS12
#server.ssl.key-alias=springboot
#server.port=8443

# Paramètres Production

## Deploiement variable Aiven sur Render
spring.datasource.url=jdbc:mysql://${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?useSSL=true&serverTimezone=UTC
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}
server.port=${PORT:8080}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

## Desactive les caches Hibernate
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

## Limite le pool de connexions Hikari
spring.datasource.hikari.maximum-pool-size=2

## Active le logging minimal
logging.level.root=WARN
logging.level.org.springframework=INFO
logging.level.org.hibernate=WARN

## Desactive les fonctionnalites inutiles
spring.main.lazy-initialization=true
spring.aop.proxy-target-class=false
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.jdbc.batch_size=20